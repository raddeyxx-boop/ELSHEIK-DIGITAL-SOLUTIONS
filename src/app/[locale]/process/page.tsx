import { notFound } from "next/navigation"; import { PageHero } from "@/components/sections/page-hero"; import { ProcessPageBody } from "@/components/process/process-page"; import { getProcessContent } from "@/content/process"; import { isLocale } from "@/lib/i18n/config";
export default async function ProcessPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const c=getProcessContent(locale);return <><PageHero eyebrow={c.hero.eyebrow} title={c.hero.title} lead={c.hero.lead} systemLabel={c.hero.systemLabel}/><ProcessPageBody content={c} locale={locale}/></>}
