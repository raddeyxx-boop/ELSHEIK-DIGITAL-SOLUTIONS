// The built-in showcase case study: prerendered by its own static segment, and the
// one slug that falls back to built-in content when the CMS cannot answer.
export const SHOWCASE_SLUG = "relax-moon-spa-automation";
