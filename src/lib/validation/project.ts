import { z } from "zod";
export const projectSchema=z.object({id:z.uuid().optional(),slug:z.string().trim().min(3).max(100).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/),title_en:z.string().trim().min(2).max(160),title_ar:z.string().trim().min(2).max(160),summary_en:z.string().trim().min(20).max(600),summary_ar:z.string().trim().min(20).max(600),industry_en:z.string().trim().max(120).optional(),industry_ar:z.string().trim().max(120).optional(),year:z.string().trim().max(20).optional(),seo_title_en:z.string().trim().max(160).optional(),seo_title_ar:z.string().trim().max(160).optional(),seo_description_en:z.string().trim().max(320).optional(),seo_description_ar:z.string().trim().max(320).optional(),canonical_url:z.union([z.url(),z.literal("")]).optional(),cover_media_id:z.union([z.uuid(),z.literal("")]).optional(),status:z.enum(["draft","published","archived"]),featured:z.boolean(),sort_order:z.number().int().min(0).max(10000)});
export const caseStudySchema=z.object({project_id:z.uuid(),overview_en:z.string().trim().max(5000).optional(),overview_ar:z.string().trim().max(5000).optional(),challenge_en:z.string().trim().max(5000).optional(),challenge_ar:z.string().trim().max(5000).optional(),objectives_en:z.string().trim().max(5000).optional(),objectives_ar:z.string().trim().max(5000).optional(),solution_en:z.string().trim().max(10000).optional(),solution_ar:z.string().trim().max(10000).optional(),security_en:z.string().trim().max(5000).optional(),security_ar:z.string().trim().max(5000).optional(),status:z.enum(["draft","published","archived"])}).superRefine((value,context)=>{if(value.status!=="published")return;for(const key of ["overview_en","overview_ar","challenge_en","challenge_ar","objectives_en","objectives_ar","solution_en","solution_ar"] as const)if(!value[key]?.trim())context.addIssue({code:"custom",path:[key],message:"Required before publishing"});});
