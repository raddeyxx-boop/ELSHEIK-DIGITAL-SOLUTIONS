import { render,screen } from "@testing-library/react";import userEvent from "@testing-library/user-event";import { describe,it,expect,vi } from "vitest";import { ContactForm } from "@/components/forms/contact-form";import { en } from "@/content/dictionaries/en";
describe("ContactForm",()=>{it("shows validation errors before submission",async()=>{const fetchMock=vi.spyOn(globalThis,"fetch");render(<ContactForm dictionary={en}/>);await userEvent.click(screen.getByRole("button",{name:"Send project brief"}));expect((await screen.findAllByText(/too small/i)).length).toBeGreaterThan(0);expect(fetchMock).not.toHaveBeenCalled();fetchMock.mockRestore()})});
