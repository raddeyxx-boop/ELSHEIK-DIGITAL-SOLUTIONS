import { notFound } from "next/navigation"; import { PageHero } from "@/components/sections/page-hero"; import { CustomerJourney } from "@/components/case-study/customer-journey"; import { getDictionary } from "@/content/dictionaries"; import { isLocale } from "@/lib/i18n/config";
export default async function ProcessPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const d=getDictionary(locale);return <><PageHero eyebrow={d.process.eyebrow} title={d.process.title} lead={locale==="ar"?"نربط قرارات المنتج والتصميم والهندسة والإطلاق ضمن مسار واحد واضح.":"Product, design, engineering, and release decisions move through one clear delivery path."}/><section className="section"><div className="shell"><CustomerJourney steps={d.process.items.map(i=>i.title)}/></div></section></>}

