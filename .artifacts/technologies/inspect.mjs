import {chromium} from '@playwright/test';import fs from 'node:fs/promises';
const browser=await chromium.launch();const rows=[];const p=await browser.newPage({deviceScaleFactor:1});
for(const locale of ['en','ar'])for(const width of [1920,1600,1440,1366,1280,1100,1024,901]){await p.setViewportSize({width,height:900});await p.goto(`http://localhost:3000/${locale}/technologies`,{waitUntil:'networkidle'});rows.push({locale,width,...await p.locator('body > header nav').first().evaluate(el=>{const a=el.querySelector('a'),r=a.getBoundingClientRect();return {labels:el.textContent,homeHref:a.getAttribute('href'),homeBounds:{x:r.x,y:r.y,width:r.width,height:r.height},zoom:visualViewport.scale,overflow:document.documentElement.scrollWidth>innerWidth};})});if(width===1440)await p.screenshot({path:`.artifacts/technologies/before-${locale}.png`,style:'nextjs-portal{display:none}'});}
await fs.writeFile('.artifacts/technologies/header-before.json',JSON.stringify(rows,null,2));console.log(rows);await browser.close();
